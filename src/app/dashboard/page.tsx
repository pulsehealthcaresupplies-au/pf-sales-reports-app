'use client';

import { SalesReportsDashboard } from '@/components/reports/SalesReportsDashboard';

/**
 * Dashboard Page
 * 
 * Main dashboard for sales reports
 * Protected by ProtectedRoute in layout
 */
export default function DashboardPage() {
  return <SalesReportsDashboard />;
}
