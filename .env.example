# Sales Reports App - Environment Variables
# Copy to .env.local and adjust. All base URLs must include protocol: http:// or https://

# =============================================================================
# Base URLs (single source - config/endpoints.ts and next.config.js rewrites)
# =============================================================================
# API Gateway (GraphQL). Use either name; NEXT_PUBLIC_API_URL is preferred.
NEXT_PUBLIC_API_URL=http://localhost:8000
# NEXT_PUBLIC_API_GATEWAY_URL=http://localhost:8000

# GraphQL path (relative) or full URL. Defaults: /graphql/sales-reports, /graphql/auth
# NEXT_PUBLIC_GRAPHQL_ENDPOINT=/graphql/sales-reports
# NEXT_PUBLIC_GRAPHQL_AUTH_ENDPOINT=/graphql/auth

# This app's origin (metadata, canonical). Include protocol.
NEXT_PUBLIC_APP_URL=http://localhost:3004

# =============================================================================
# WebSocket (optional; derived from API base if unset)
# =============================================================================
# NEXT_PUBLIC_WS_URL=ws://localhost:8000/graphql/sales-reports
# NEXT_PUBLIC_WS_AUTH_URL=ws://localhost:8000/graphql/auth
# NEXT_PUBLIC_WS_NOTIFICATIONS_URL=ws://localhost:8000/ws/notifications

# =============================================================================
# Deployment
# =============================================================================
NODE_ENV=development

# =============================================================================
# Next.js Server Actions (production)
# =============================================================================
# Generate once: openssl rand -base64 32. Set at BUILD and RUNTIME (same value).
# NEXT_SERVER_ACTIONS_ENCRYPTION_KEY=
